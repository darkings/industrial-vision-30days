# 学习进度与教学约定

最后更新：2026-06-18

## 学习背景

- 已有 JavaScript、Node.js、npm、模块化、API 调用和基础异步编程经验。
- Python 没有系统学习过，可以阅读并根据示例修改代码。
- 学习目标不是算法研究，而是工业机器视觉项目落地。
- 每天可高强度学习约 5 小时。
- 1 个月后需要向目标公司展示学习成果。

## Python 环境策略

项目统一使用根目录虚拟环境：

```text
/home/jie/Projects/industrial-vision-30days/.venv
```

环境原则：

- 不按天或按周重复创建环境。
- OpenCV、YOLO、PaddleOCR 和综合项目优先共用根环境。
- 新依赖安装后同步更新 `requirements.txt`。
- 只有发生明确且无法解决的依赖冲突时，才建立专用环境。
- 专用环境必须在 README 中记录用途、依赖和启动方法。
- 换电脑时根据 `requirements.txt` 重新创建，不复制旧 `.venv`。

2026-06-18 环境迁移结果：

```text
根环境 Python：3.12.3
opencv-python：4.13.0.92
numpy：2.4.6
matplotlib：3.11.0
旧 day01/.venv：已删除
旧环境 Git 跟踪文件：4518 个已取消跟踪
```

激活命令：

```bash
cd ~/Projects/industrial-vision-30days
source .venv/bin/activate
```

已有 Day 1、Day 2 脚本已使用根环境完成语法、非交互运行和 OpenCV 处理逻辑验证。

## 最终目标

完成一个可展示的工业视觉综合 Demo：

```text
图片或摄像头输入
-> OpenCV 图像预处理
-> YOLO 目标或缺陷检测
-> PaddleOCR 字符识别
-> 规则判断
-> 输出 OK/NG
-> 保存结果图、CSV 和日志
```

## 当前进度

### Day 1：OpenCV 图像基础

状态：核心基础已完成，原计划中的第 6、7 步移动到 Day 2。

已经学习和练习：

- 使用 `cv2.imread()` 读取图片。
- 理解图片是 `NumPy` 数组。
- 理解 `image.shape` 的顺序是高度、宽度、通道数。
- 理解 OpenCV 默认使用 BGR 通道顺序。
- 获取图片宽度、高度、通道数和中心像素值。
- 使用 `cv2.cvtColor()` 将彩色图片转换为灰度图。
- 理解 ROI 是感兴趣区域。
- 使用 `image[y1:y2, x1:x2]` 裁剪 ROI。
- 从代表图片确定固定 ROI，再批量保存 ROI 图片。
- 使用 `pathlib.Path` 处理项目路径。
- 对图片读取失败进行 `None` 检查。

现有练习文件：

```text
week01-opencv/day01/test03_read_imgs.py
week01-opencv/day01/test04_gray_img.py
week01-opencv/day01/test05_roi_img.py
```

当前测试数据：

- 输入图片：9 张 PNG。
- 灰度输出：9 张。
- ROI 输出：9 张。

需要继续巩固：

- `image.shape` 返回的是 `(height, width, channels)`。
- 像素访问顺序是 `image[y, x]`。
- ROI 裁剪顺序是 `image[y1:y2, x1:x2]`。
- OpenCV 画框使用的点坐标却是 `(x, y)`。
- 使用新 API 前，必须能解释每个参数。

## 下一课：Day 2

Day 2 从 Day 1 原计划的第 6、7 步开始。

### 第一部分：画检测框

状态：已完成并通过运行与输出图片检查。

已经掌握：

- 为什么要先使用 `marked = image.copy()`。
- `cv2.rectangle()` 的作用。
- 左上角 `(x1, y1)` 与右下角 `(x2, y2)`。
- BGR 颜色，例如绿色 `(0, 255, 0)`。
- 线条粗细参数。
- 使用 `cv2.putText()` 写入 ROI、OK、NG 等文字。
- 将标注图保存到 `outputs/marked`。
- 使用图片宽高和中心点动态计算检测框坐标。
- 使用 `max()` 避免文字坐标过于靠近图片顶部。
- 检查 `cv2.imwrite()` 返回的保存结果。

完成文件：

```text
week01-opencv/day02/test01_draw_rectangle.py
```

输出结果：

```text
week01-opencv/day02/outputs/marked/IMG_9850.png
```

### 第二部分：批量处理

状态：已完成并通过运行、计数和输出数量检查。

已经掌握：

- `for` 循环的基本写法。
- `Path.iterdir()` 返回什么。
- `Path.is_file()` 为什么要加括号。
- `Path.suffix.lower()` 如何过滤图片类型。
- 为什么需要检查 `cv2.imread()` 是否返回 `None`。
- 如何为每张输入图生成对应输出文件名。
- 如何统计处理成功、失败和跳过的图片数量。
- 使用 `continue` 跳过当前失败或不支持的文件。
- 区分读取失败、保存失败和主动跳过。
- 使用 `max()`、`min()` 限制检测框坐标边界。

完成文件：

```text
week01-opencv/day02/test02_batch_mark_images.py
```

验证结果：

```text
输入图片：10
输出图片：10
处理成功：10
处理失败：0
主动跳过：0
```

### 第三部分：图像预处理

状态：滤波基础已完成并通过代码、文件数量和像素差异检查。

已经掌握：

- 噪声和滤波的基本作用。
- 均值滤波、高斯滤波和中值滤波的基本区别。
- 均值滤波允许矩形核。
- 高斯滤波核宽高通常必须是正奇数。
- 中值滤波只传入一个正奇数边长。
- `sigmaX=0` 表示让 OpenCV 根据核尺寸自动计算。
- 核越大，平滑越强，同时越容易损失边缘和小缺陷。
- 不同处理结果必须使用不同输出文件名，避免相互覆盖。

完成文件：

```text
week01-opencv/day02/test03_image_filters.py
```

输出结果：

```text
IMG_9851_mean.png
IMG_9851_gaussian.png
IMG_9851_median.png
```

验证结果：

```text
脚本运行成功
三种结果均保存成功
三张结果像素内容互不相同
```

下一节：

```text
亮度与对比度调整：已完成
```

### 第四部分：亮度与对比度

状态：已完成并通过代码运行、文件输出和像素统计检查。

已经掌握：

- 使用 `cv2.convertScaleAbs()` 调整图像。
- `alpha` 主要控制对比度。
- `beta` 主要控制亮度。
- 使用不同文件名保存不同参数的处理结果。
- 使用 `all()` 判断多个保存操作是否全部成功。
- 参数过大会导致大量像素达到 `255`，使高光区域丢失细节。
- 软件增强不能替代正确的光源、曝光和增益设置。

完成文件：

```text
week01-opencv/day02/test04_brightness_contrast.py
```

下一节：

```text
灰度直方图与直方图均衡化
```

### 第五部分：灰度直方图与均衡化

状态：已完成并通过代码运行、输出文件、图像效果和理论问答检查。

已经掌握：

- 直方图横轴表示灰度值，纵轴表示对应灰度值的像素数量。
- 直方图集中在左侧通常表示图片整体偏暗。
- 使用 `cv2.calcHist()` 统计灰度分布。
- 使用 `cv2.equalizeHist()` 进行全局直方图均衡化。
- 使用 CLAHE 进行受限制的局部对比度增强。
- `clipLimit` 过大会放大噪声和局部纹理。
- 使用 `plt.close()` 释放画布资源，避免批量绘图时持续占用内存。
- 使用 `Path.stem` 从输入图片自动生成可追溯的输出文件名。

完成文件：

```text
week01-opencv/day02/test05_histogram_equalization.py
```

验证结果：

```text
彩色图 shape：(853, 640, 3)
灰度图 shape：(853, 640)
三个直方图 shape：(256, 1)
IMG_9859 的四个结果文件均成功生成
```

下一节：

```text
图像锐化
```

### 第六部分：图像锐化

状态：已完成并通过代码运行、输出文件、图像效果和理论问答检查。

已经掌握：

- 使用高斯模糊图估计图像中的平缓、低频成分。
- 使用 `cv2.addWeighted()` 实现反遮罩锐化。
- 普通锐化公式 `1.5 * 原图 - 0.5 * 模糊图`。
- 强锐化公式 `2.0 * 原图 - 1.0 * 模糊图`。
- 锐化会增强边缘，也会增强噪声和无关纹理。
- 过度锐化可能产生黑边、白边和伪缺陷。
- `cv2.addWeighted()` 最后一个参数是额外偏移量。

完成文件：

```text
week01-opencv/day02/test06_image_sharpening.py
```

输出结果：

```text
IMG_9855_blurred.png
IMG_9855_sharpened.png
IMG_9855_strong_sharpened.png
```

验证观察：

```text
原图标准差：55.368
模糊图标准差：54.711
普通锐化标准差：55.874
强锐化标准差：56.452

锐化越强，纯黑和纯白像素越多，边缘对比度提高，
同时发生像素截断和伪边缘的风险也越高。
```

## Day 2 完成状态

状态：已完成。

Day 2 已完成：

- 检测框和文字标注。
- 批量图片处理。
- 均值、高斯和中值滤波。
- 亮度与对比度调整。
- 灰度直方图、全局均衡化和 CLAHE。
- 反遮罩图像锐化。

Day 3 起点：

```text
固定阈值二值化
OTSU 自动阈值
自适应阈值
形态学基础
```

## 固定教学方法

从 Day 2 开始，每个知识点必须按照以下顺序进行：

1. 说明这个知识点解决什么工业问题。
2. 讲解本节会用到的 Python 语法。
3. 说明第三方包、模块、函数和参数的作用。
4. 提供一份可以直接运行的完整示范代码。
5. 对完整代码进行逐段或逐行解释。
6. 学员跟写一次，不要求脱离示例。
7. 修改参数并观察结果变化。
8. 完成一个小型模仿练习。
9. 最后完成一个独立练习。
10. 检查代码、运行结果和理论理解后再进入下一节。

不允许直接布置尚未讲解过的内容。如果练习需要新的 Python 语法或 OpenCV API，必须先教学再练习。

## 错误知识库维护规则

项目根目录的 `LEARNING_ERRORS.md` 是整个 30 天学习过程持续维护的错误知识库。

以后每次检查练习时：

1. 在聊天中直接指出错误，由学员亲自修改。
2. 说明错误现象、根本原因和正确写法。
3. 将新错误追加到 `LEARNING_ERRORS.md`。
4. 已记录过的同类错误再次出现时，标记为“重复错误”，并记录再次发生的日期和文件。
5. 不能只记录答案，必须记录为什么会错以及以后如何避免。
6. 普通变量名、文件名、注释等纯拼写错误只在聊天中提醒，不写入错误知识库。
7. 错误知识库只记录有学习价值的概念、API、控制流、数据处理和工程逻辑问题。

## Obsidian 笔记检查规范

以后检查 Day 笔记时，必须同时检查四个层面：

### 1. 知识正确性

- 理论描述是否准确。
- API 参数、返回值和限制条件是否正确。
- 是否存在容易形成错误理解的过度简化。
- 工业应用场景和风险是否说明清楚。

### 2. 代码正确性

- 示例代码是否具备正确语法。
- 控制语句是否缺少冒号。
- 路径、坐标、通道和 shape 顺序是否正确。
- 代码块是否能够独立理解。
- 注释是否与代码一致。

### 3. Markdown 美观与可读性

- 标题层级是否连续、统一，避免一级标题和三级标题混乱使用。
- 同级内容是否使用相同级别标题。
- 段落是否过长、过密，是否需要拆分。
- 列表是否适合快速扫描。
- 对比信息是否适合改成表格。
- 代码块是否标注语言，例如 `python`、`text`、`bash`。
- 代码围栏是否成对闭合。
- 行内代码、变量名、API 名称是否使用反引号。
- 中文、英文、数字和标点之间的排版是否自然。
- 是否存在多余空行、孤立符号、残留反引号或错误缩进。
- 核心结论是否突出，是否方便在 Obsidian 中快速复习。

### 4. 内容完整性与工业补充

- 是否记录当天真正练习过的内容。
- 是否记录实验结论和参数副作用。
- 是否说明误检、漏检、数据追溯和稳定性风险。
- 是否补充硬件、光源、相机和治具对算法的影响。
- 是否包含当天核心记忆或复习清单。

Markdown 修改原则：

- 保留学员原有表达和学习痕迹。
- 优先改善结构、可读性和准确性，不把笔记改成陌生的教科书。
- 普通排版和拼写问题直接修正，不写入 `LEARNING_ERRORS.md`。

## 每课完成后的项目同步

每一课验收通过后，必须完成以下收尾工作：

1. 更新项目根目录 `README.md`。
2. 更新 `README.md` 的最后更新时间、课程状态、已完成练习和下一课。
3. 更新 `LEARNING_PROGRESS.md` 的完成状态、掌握内容和验证结果。
4. 将有学习价值的新错误追加到 `LEARNING_ERRORS.md`。
5. 检查并完善当天 Obsidian 笔记。

只有代码、理论、输出结果和项目文档全部同步后，该课才算正式完成。

## 教学代码注释规范

以后所有用于教学的代码必须包含详细中文注释。

注释至少需要说明：

- 导入的包或模块有什么作用。
- 变量保存的是什么数据。
- 函数解决什么问题。
- OpenCV API 的每个参数表示什么。
- 图片数据的 shape 和通道发生了什么变化。
- 坐标使用 `(x, y)` 还是 `[y, x]`。
- 为什么要判断 `None`。
- 输入文件来自哪里，输出文件保存到哪里。
- 容易写错或容易混淆的地方。

示例：

```python
# cv2.rectangle() 会直接修改传入的图片。
# 因此先复制原图，避免后续仍然需要无标记的原始图片时数据已经被改变。
marked = image.copy()

# 在 marked 图片上绘制绿色矩形框。
cv2.rectangle(
    marked,          # 要绘制的图片，是一个 NumPy 数组
    (x1, y1),        # 矩形左上角坐标，顺序是 (x, y)
    (x2, y2),        # 矩形右下角坐标，顺序是 (x, y)
    (0, 255, 0),     # BGR 颜色：蓝0、绿255、红0，因此是绿色
    2                # 线条粗细，单位是像素
)
```

详细注释用于学习阶段。等知识掌握后，正式项目代码会减少重复注释，只保留解释设计原因和复杂逻辑的注释。

## 每节课验收方式

完成一节课后，需要检查三个层次。

理论理解：

- 能用自己的话解释本节知识。
- 能说明它适合解决什么工业问题。
- 能指出常见错误。

代码理解：

- 能解释关键代码和参数。
- 能修改路径、坐标、颜色、阈值等参数。
- 发生错误时能读懂基础报错位置。

实际结果：

- 使用自己的图片运行成功。
- 输出文件数量正确。
- 输出图片可以正常打开。
- 失败案例被记录，而不是删除或忽略。

## 换电脑或换聊天后的恢复提示

开始新聊天时，可以发送：

```text
请先阅读项目根目录的 README.md 和 LEARNING_PROGRESS.md。
我是 Python 初学者，但有 JavaScript/Node.js 基础。
所有教学代码必须有详细中文注释。
请按照“理论 -> Python语法 -> API参数 -> 完整示范 -> 跟写 -> 修改 -> 独立练习 -> 验收”的顺序继续。
当前从 README.md 中记录的“当前下一课”继续。
```

## Obsidian 笔记

Day 1 笔记位置：

```text
C:\Users\Jie\iCloudDrive\iCloud~md~obsidian\SecondBrain\raw\industrial\Day 1.md
```

笔记中已经修正：

- 图像 shape 的高宽顺序。
- 像素访问的 `[y, x]` 顺序。
- ROI 的 `[y1:y2, x1:x2]` 顺序。
- 二值化的阈值说明。
