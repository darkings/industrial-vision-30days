# OpenCV 的 Python 包，用于读图、图像处理、画框、摄像头读取等。
# Ultralytics 依赖 opencv-python，因此保留该包。
opencv-python==4.13.0.92

# PaddleOCR/PaddleX 会使用 contrib 版 OpenCV。当前实际 import cv2 显示为 4.10.0。
opencv-contrib-python==4.10.0.84

# 数组和矩阵运算库。OpenCV 读取的图片本质上是 NumPy 数组。
# 固定在 2.3.x：满足 OpenCV/YOLO，同时兼容 PaddleOCR 依赖 paddlex 的 numpy<2.4 要求。
numpy==2.3.5

# 学习阶段用于显示图片、绘图和比较处理结果。
matplotlib==3.11.0

# Matplotlib 的轮廓绘图依赖。显式固定 Windows wheel 版本，避免 PaddleOCR 依赖解析时回退到需要本机编译的旧版本。
contourpy==1.3.3

# YOLO 目标检测库。Day15-Day17 用于推理、训练烟测、批量报告和失败样本筛查。
ultralytics==8.4.90

# PaddleOCR 字符识别库。Day18 起用于 OCR 最小示例、文字区域识别和结果解析。
paddleocr==3.7.0

# PaddleOCR 的 CPU 推理后端。当前 Windows 学习环境优先使用 CPU 版本。
paddlepaddle==3.3.1

# 现代 Python 数据验证库，利用类型提示在程序启动时提供严苛的运行时参数校验。
pydantic==2.8.2

# Pydantic 官方的配置管理扩展包，支持从环境变量、.env 文件、甚至 JSON/YAML 动态读取和反序列化配置。
pydantic-settings==2.3.4